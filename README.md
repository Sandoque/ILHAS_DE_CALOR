# Observatório Estadual de Ilhas de Calor – PE

Projeto inicial com Flask, HTMX, ECharts e pipeline ETL para dados do INMET.

## Como rodar
1. python -m venv venv
2. venv\\Scripts\\activate
3. pip install -r backend/requirements.txt
4. cp backend/.env.example backend/.env
5. python backend/run.py
